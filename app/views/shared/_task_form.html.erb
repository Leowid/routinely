<%= form_for(@task) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.time_select("hour") %> - <%= f.time_select("to_hour", include_blank: true) %>
	</div>
	<div class="textfield">		
		<%= f.text_field :content, placeholder: "Add new item..." %>
	</div>
	<%= f.submit "Save", class: "btn btn-large btn-primary" %>
<% end %>